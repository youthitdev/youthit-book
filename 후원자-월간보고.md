# 후원자에게 다달이 보내는 것

후원자 로그인은 아직 없다(도너스 연동 방식 미정). 그래서 **로그인 없이 되는
것**으로 시작한다 — 문자 한 통과 `sponsor.html` 링크.

이건 어차피 해야 하는 일이기도 하다. 기부금품법상 **연간 사용명세 공시**가
필요한데, 후원자에게 보내는 내용과 같다. 분기 네 통을 모으면 연간 보고가 된다.

---

## 금액을 환산하지 않는다

한때 「1만원 = 열두 밤」 같은 환산을 넣었다가 뺐다. 두 가지 이유다.

**「밤」은 만든 말이다.** 앱은 「인증」, 「하루 한 장」, 「3일째」라고 말한다.
후원자는 「열두 밤」을 읽고 그게 무슨 일인지 그릴 수 없다.

**1만원은 이 프로그램의 어디에도 안 맞는다.** 도서기금으로 8,000원이 가고 책은
평균 16,500원이니 0.48권이다. 말을 바꿔서 해결될 일이 아니었다.

그래서 **개인 금액을 환산하지 않는다.** 이번 달에 무슨 일이 있었는지만 쓴다.

### ⚠️ 이렇게 쓰면 안 된다

- ~~「덕분에 책 한 권이 전해졌어요」~~ — 1만원 후원자에게는 사실이 아니다
- ~~「1만원이면 아이가 12일 읽어요」~~ — 읽으면 돈을 주는 것처럼 읽힌다
- ~~「당신의 후원으로 이 책을 샀습니다」~~ — 돈은 섞여 있다. 특정 후원자에게
  귀속시키면 나중에 지정기부 다툼이 생긴다

순서를 지킨다. **아이가 먼저 읽고, 후원이 그걸 책으로 바꾼다.**

---

## 숫자는 손으로 모으지 않는다

관리자 화면 **「월간 보고」 탭**에서 월을 고르고 「불러오기」를 누르면 끝이다.
`dokseo_monthly_report(연, 월)` 하나가 다 모은다 — 읽은 청소년, 인증, 처음
인증한 사람, 루틴 참여, 전해진 책, 책값, 남은 도서기금, 책방별 권수,
**아이들이 읽은 책 목록**, 공개로 켠 후기와 문장.

문자 본문도 그 자리에서 만들어진다. **복사 버튼 하나.**

매달 쿼리를 여러 개 돌려 숫자를 옮겨 적으면 반드시 틀린다.

### 숫자가 0 인 줄은 문자에서 빠진다

「이번 달 책 0권」을 굳이 쓰지 않는다. 아무 일도 없었다는 인상을 준다.
`rpMessage()` 가 0 인 줄을 걸러낸다.

---

## 문자 문안

문자는 45자가 넘으면 LMS다. 아래는 관리자 화면이 만들어주는 모양이다.

```
[유스보이스] 한끗독서 2026년 3분기

○○○님, 함께해주셔서 고맙습니다.

이번 분기에 청소년 12명이 340일을 읽었고,
동네책방에서 책 9권을 골라 갔습니다.

한 아이가 옮겨 적은 문장입니다.
"내일도 이만큼만 하면 되겠다."

무엇에 쓰였는지 여기서 보실 수 있어요.
https://youthitdev.github.io/youthit-book/sponsor.html

무료수신거부 0808-000-000
```

**바꿔 넣는 자리는 넷뿐이다.** 이름 · 사람 수와 날 수 · 책 권수 · 문장 하나.

### 문장은 반드시 갈아 끼운다

분기마다 다른 문장을 넣는다. 같은 문장을 두 번 보내면 **자동 발송으로 읽힌다.**
운영진이 공개로 고른 것 중에서 고르면 된다(관리자 → 문장·후기 고르기).

---

## 숫자 가져오기

```sql
-- 분기 성과. 날짜만 바꿔 쓴다
SELECT
  (SELECT count(DISTINCT user_id) FROM certifications
    WHERE cert_date BETWEEN '2026-07-01' AND '2026-09-30')          AS 읽은청소년,
  (SELECT count(*) FROM certifications
    WHERE cert_date BETWEEN '2026-07-01' AND '2026-09-30')          AS 읽은날,
  (SELECT count(*) FROM book_purchases
    WHERE status = 'settled'
      AND settled_at::date BETWEEN '2026-07-01' AND '2026-09-30')   AS 전해진책,
  (SELECT COALESCE(sum(amount), 0) FROM book_purchases
    WHERE status = 'settled'
      AND settled_at::date BETWEEN '2026-07-01' AND '2026-09-30')   AS 책값합계;

-- 보낼 문장 고르기 (공개로 켠 것 중 최근 것)
SELECT quote, book_title, cert_date
  FROM certifications
 WHERE quote_public AND COALESCE(btrim(quote), '') <> ''
 ORDER BY cert_date DESC LIMIT 10;
```

---

## 받는 사람

도너스에서 뽑는다. **공개 동의와 무관하게 전원에게 보낸다** — 사용명세를
알리는 건 동의 여부와 상관없는 의무다.

수신거부 번호는 반드시 넣는다. 정보통신망법상 영리 목적 광고성 정보가 아니라
사용명세 보고이므로 광고 표기는 필요 없지만, **수신거부 수단은 넣는 편이 안전하다.**

---

## `sponsor.html` 에 이미 들어 있는 것

링크를 열면 보이는 것들이다. 문자에서 반복할 필요가 없다.

| 자리 | 무엇 |
|---|---|
| 맨 위 | **문장 하나** — 숫자보다 먼저 눈에 들어오게 따로 세웠다 |
| 만원이면 무엇이 되나요 | 1·3·5·10만원 → 밤. **실제로 들어온 돈과 실제로 산 책값으로 계산한다** |
| 지금까지 함께 만든 것 | 응원 어른 · 쌓인 독서 · 전해진 책 · 모인 후원금 |
| 책이 도착한 날 | 아이가 책으로 얼굴을 가리고 찍은 사진 |
| 아이들이 멈춰 선 문장 | 나머지 문장들 |
| 후원금은 이렇게 쓰입니다 | 비율과 정책 |

「만원이면」 칸의 숫자는 **가정한 값이 아니다.** `dokseo_pool_status()` 의
`book_fund / total_donated` 로 비율을, `spent / books_bought` 로 책값을 낸다.
책을 비싸게 사면 밤이 줄어든다. 그대로 드러난다.

---

## 아직 안 한 것

- **분기 보고를 보낸 기록**을 어디에도 남기지 않는다. 누구에게 언제 보냈는지
  모른다. 후원자가 늘면 필요해진다
- 연간 사용명세 **공시** 형식은 지자체마다 다르다. 세무사/지자체 확인 필요
- 후원자 로그인이 생기면 「당신이 함께한 뒤로」 칸이 살아난다. 지금은 숨겨져 있다
