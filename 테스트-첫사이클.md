# 첫 사이클 한 번 돌려보기

조각은 다 만들었는데 **처음부터 끝까지 이어서 돌아간 적이 없다.**
특히 **후기 30P** 가 붙은 뒤로 250P 문턱이 실제로 넘어가는지는 해봐야 안다.

계정: `teen@youthvoice.or.kr`(청소년) · `kkut@`(끗짱) · `yv@`(운영진)

---

## 0단계 — 지금 어디까지 왔나

```sql
SELECT u.email,
       p.name,
       (SELECT count(*) FROM certifications c WHERE c.user_id = u.id)            AS 인증수,
       (SELECT count(*) FROM reviews v WHERE v.user_id = u.id)                   AS 후기수,
       (SELECT count(*) FROM book_purchases b
         WHERE b.user_id = u.id AND b.status <> 'void')                          AS 받은책,
       dokseo_points(u.id) ->> 'points'  AS 총포인트,
       dokseo_points(u.id) ->> 'left'    AS 쓸수있는교환권,
       dokseo_points(u.id) ->> 'to_next' AS 다음까지
  FROM auth.users u JOIN profiles p ON p.id = u.id
 WHERE p.role = 'youth'
 ORDER BY 3 DESC;
```

> ⚠️ 루틴 1 은 `points_per_cert = 20` 이다. migration-12 로 기본값이 10 이 되기
> 전에 만들어져 그 값이 굳어 있다. **지금 만드는 루틴은 10P** 이므로, 여기 나오는
> 포인트는 실제보다 두 배로 보인다. 숫자를 그대로 믿지 말 것

---

## 1단계 — 루틴 후기 (+30P) ⭐ 한 번도 안 해봄

루틴 1 「하루 10분 책읽기」가 `done` 이라 지금 쓸 수 있다.

1. `teen@` 로 로그인 → **후기 탭**
2. 맨 위 **「✍️ 쓸 수 있는 후기」** 에 루틴 1 이 떠야 한다
3. 눌러서 20자 이상 쓰고 올리기
4. **MY 탭** → 포인트가 **+30P**, 출처에 **📝 후기 30P** 가 새로 보여야 한다

**안 뜨면** — `routineEnded()` 판정(종료일이 지났거나 `status='done'`)이나
참여자 여부를 본다.

```sql
SELECT r.id, r.title, r.status, r.end_date,
       (SELECT count(*) FROM routine_participants p
         WHERE p.routine_id = r.id AND p.status = 'approved') AS 참여자
  FROM routines r ORDER BY r.id;
```

---

## 2단계 — 책인증후기 (+30P) ⭐ 한 번도 안 해봄

이미 받은 책 1권이 있으니 바로 쓸 수 있다.

1. **후기 탭** → 「📗 받은 책 후기를 남겨주세요」
2. 책 제목 + 20자 이상
3. MY 탭에서 **+30P** 확인

---

## 3단계 — 두 번째 교환권까지 채우기

교환권은 250P 마다 한 장이다. 이미 한 장을 썼으므로 **500P** 를 넘겨야 다음이 나온다.
하루 한 번만 인증할 수 있어 오늘 눌러서는 못 채우므로, 지난 날짜로 밀어 넣는다.

```sql
-- ⚠️ 테스트용. 끝나면 아래 '정리' 로 반드시 지울 것
INSERT INTO certifications
  (routine_id, user_id, photo_urls, quote, book_title, page_end, content, cert_date, created_at)
SELECT p.routine_id, p.user_id, '{}'::text[],
       '[테스트] 오늘도 한 장 읽었다', '테스트 책', 10 * g, '[테스트] 채워 넣은 기록',
       ((now() AT TIME ZONE 'Asia/Seoul')::date - g),
       now() - (g || ' days')::interval
  FROM routine_participants p
  CROSS JOIN generate_series(15, 30) AS g     -- 0단계에서 이미 찬 날짜 뒤로
 WHERE p.routine_id = 1 AND p.status = 'approved'
ON CONFLICT DO NOTHING;
```

그리고 0단계 쿼리를 다시 돌려 **`쓸수있는교환권 = 1`** 이 됐는지 본다.
누적 30일을 넘기면 마일스톤 **+10P** 도 붙는다.

---

## 4단계 — 책 받았어요

1. `teen@` → **MY 탭** 에 「🎫 쓸 수 있는 교환권 1장」 과 **[책 받았어요]** 버튼
2. 누르고 책방 고르기 → 책 사진(얼굴은 책으로 가리고) → 공개 동의는 켜 본다
3. 올리면 「📗 받은 책을 확인하고 있어요」 로 바뀌어야 한다

```sql
SELECT id, user_id, bookstore_id, status, amount, photo_public, created_at
  FROM book_purchases ORDER BY id DESC LIMIT 3;
```
→ 새 줄이 `pending` 으로 있어야 한다

---

## 5단계 — 정산 (운영진)

1. `yv@` 로 **관리자 화면** → **정산 대기** 탭에 1건
2. 금액 입력(예: 15,000) + 영수증 사진 → **정산 확정**
3. 위 숫자판에서 **책이 된 금액**이 늘고 **남은 도서기금**이 줄어야 한다

```sql
SELECT * FROM dokseo_pool_status();
```

---

## 6단계 — 서점 입금

1. **서점 입금** 탭 → 「아직 안 보낸 곳」에 그 서점과 금액
2. 실제로 이체한 뒤 **입금함** 표시

```sql
SELECT id, bookstore_id, amount, status, settled_at, paid_at
  FROM book_purchases ORDER BY id DESC LIMIT 3;
```
→ `paid_at` 이 채워져야 한다

---

## 7단계 — 사진 공개 승인 · 문장/후기 고르기

1. **정산 대기 / 서점 입금** 탭에서 책 사진 **공개 승인**
2. **문장 · 후기 고르기** 탭에서 문장 하나, **후기 하나**를 공개로

---

## 8단계 — 후원자 화면

`sponsor.html` 을 열어 확인한다.

- 상단 지표 — 응원 어른 / 쌓인 독서 / 전해진 책 / 모인 후원금
- **「책이 도착한 날」** 에 방금 승인한 사진
- 요즘 옮겨 적은 문장
- 공개로 켠 **후기**

```sql
SELECT * FROM dokseo_public_reviews(6);
```

---

## 끝나고 — 테스트 기록 지우기

```sql
DELETE FROM certifications WHERE content = '[테스트] 채워 넣은 기록';
```

후기·구매 기록은 남는다. 지우려면 손으로 지울 것.

---

## 보면서 적어둘 것

- [ ] 1단계 — 후기 탭에 「쓸 수 있는 후기」가 떴나
- [ ] 1·2단계 — 서버 포인트(`dokseo_points`)와 앱 MY 화면 숫자가 **같은가**
- [ ] 3단계 — 500P 에서 교환권이 정확히 한 장 더 나왔나
- [ ] 5단계 — 정산 뒤 남은 도서기금이 맞게 줄었나
- [ ] 8단계 — 후원자 화면에 사진·문장·후기가 다 나왔나
